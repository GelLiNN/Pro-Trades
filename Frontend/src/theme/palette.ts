import type {ThemeOptions} from '@mui/material'

export const palette: ThemeOptions['palette'] = {
  // primary: {
  // main: '#ffffff',
  // },
  // secondary: {
  // main: '#ffffff',
  // },
}
