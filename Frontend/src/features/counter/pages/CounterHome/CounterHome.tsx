import {Layout} from '@/components/Layout'
import {Counter} from '@/features/counter/components/Counter'

export const CounterHome = () => {
  return (
    <Layout>
      <Counter />
    </Layout>
  )
}
